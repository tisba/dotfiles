# Control, where Go installs packages.
# via https://go.dev/wiki/SettingGOPATH:
#   If no GOPATH is set, it is assumed to be $HOME/go on Unix systems
export GOPATH="$HOME/Documents/src/golang"

export PATH="$GOPATH/bin:$PATH"
