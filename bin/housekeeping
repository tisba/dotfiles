#!/bin/bash

set -e

echo "=== Homebrew Cleanup"
brew cleanup -s && rm -rf $(brew --cache)/*

echo "=== brew doctor"
brew doctor

echo "=== RVM Cleanup"
rvm get head
rvm cleanup all

echo "=== update tldr"
tldr --update
