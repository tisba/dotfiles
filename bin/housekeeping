#!/bin/bash

set -e

echo "=== Homebrew Updates"
brew upgrade
brew cask upgrade

echo "=== Homebrew Cleanup"
brew_cache=$(brew --cache)
brew cleanup -s && rm -rf "${brew_cache:?}"/*

echo "=== brew doctor"
brew doctor

echo "=== RVM Cleanup"
rvm get head
rvm cleanup all
